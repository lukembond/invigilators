---
const types = [
  { value: "all", label: "All" },
  { value: "aural-homework", label: "Aural Homework" },
  { value: "mykonos", label: "Mykonos Sessions" },
];
---

<header 
  id="nav" 
  class="sticky top-0 z-50 bg-white/5 backdrop-blur-md py-4 transition-opacity duration-300"
  style="position: sticky; top: 0;"
>
  <div class="container mx-auto px-4 flex items-center justify-between">
    <h1 class="text-[1.125rem] md:text-[2rem] font-bold text-white">
      <a href="/" class="logo-text transition-colors">
        <span class="text-[0.75rem] md:text-[0.875rem]">the</span> Invigilators
      </a>
    </h1>
    <div class="flex items-center gap-2 md:gap-3">
      <div class="flex items-center gap-0.5 md:gap-1">
        {types.map((type) => (
          <button
            class={`filter-btn cursor-pointer text-[0.5rem] md:text-[0.75rem] px-1.5 md:px-3 py-0.5 md:py-1 rounded border transition-all ${
              type.value === "all" 
                ? "bg-white text-black border-white" 
                : "text-white/70 border-white/20 hover:border-white/50"
            }`}
            data-filter={type.value}
          >
            {type.label}
          </button>
        ))}
      </div>
      <div class="flex items-center gap-0.5 hidden md:visible">
        <button id="nav-up" class="nav-btn text-white/70 hover:text-white p-1 md:px-2 md:py-1 rounded border border-white/20 bg-white/5 transition-all" aria-label="Previous episode">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 md:h-4 md:w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" />
          </svg>
        </button>
        <button id="nav-down" class="nav-btn text-white/70 hover:text-white p-1 md:px-2 md:py-1 rounded border border-white/20 bg-white/5 transition-all" aria-label="Next episode">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 md:h-4 md:w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>
      </div>
    </div>
  </div>
</header>
