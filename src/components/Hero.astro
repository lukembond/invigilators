---
const articleLetters = "The".split("");
const titleLetters = "Invigilators".split("");
---

<section
  id="landing"
  class="landing-view pointer-events-none relative flex h-screen w-screen items-center justify-center overflow-hidden bg-(--background) opacity-100 transition-opacity duration-260"
>
  <video
    id="video_background"
    autoplay
    loop
    muted
    playsinline
    preload="auto"
    class="landing-video pointer-events-none absolute inset-0 h-full w-full object-cover opacity-[0.72]"
  >
    <source src="/video/invigivideo-1-2009-bg-av1.webm" type='video/webm; codecs="av1"' />
    <source src="/video/invigivideo-1-2009-bg-h265.mp4" type='video/mp4; codecs="hvc1"' />
    <source src="/video/invigivideo-1-2009-bg.mp4" type="video/mp4" />
    <source src="/video/invigivideo-1-2009.mp4" type="video/mp4" />
    <source src="/video/invigivideo-1-2009.webm" type="video/webm" />
  </video>

  <div class="landing-overlay pointer-events-none absolute inset-0"></div>

  <div
    class="landing-content relative z-1 flex flex-col items-center gap-12 px-6 max-[700px]:gap-8"
  >
    <h1
      class="landing-wordmark flex w-[min(94vw,900px)] flex-col items-center leading-none text-(--foreground) max-[700px]:w-[min(86vw,340px)]"
      aria-label="The Invigilators"
    >
      <span
        class="wordmark-line wordmark-line--article block whitespace-nowrap text-[clamp(28px,4vw,52px)] font-extralight uppercase max-[700px]:text-[clamp(22px,7vw,30px)]"
        aria-hidden="true"
      >
        {
          articleLetters.map((letter, index) => (
            <span
              class="wordmark-letter inline-block translate-y-[0.28em] opacity-0"
              style={`--letter-index: ${index}`}
            >
              {letter}
            </span>
          ))
        }
      </span>
      <strong
        class="wordmark-line wordmark-line--title block whitespace-nowrap text-[clamp(38px,6vw,80px)] font-bold uppercase max-[700px]:text-[clamp(28px,7.5vw,34px)]"
        aria-hidden="true"
      >
        {
          titleLetters.map((letter, index) => (
            <span
              class="wordmark-letter inline-block translate-y-[0.28em] opacity-0"
              style={`--letter-index: ${index + articleLetters.length + 1}`}
            >
              {letter}
            </span>
          ))
        }
      </strong>
    </h1>
    <div class="landing-rule h-px w-30 origin-center bg-(--primary)" aria-hidden="true"></div>
    <button
      id="enter-btn"
      class="enter-btn pointer-events-auto relative z-2 isolate cursor-pointer overflow-hidden border border-[rgba(240,228,228,0.35)] bg-transparent px-10 py-3.5 text-[11px] font-normal uppercase tracking-[0.3em] text-(--foreground) indent-[0.3em] transition-[border-color,background,box-shadow] duration-300"
      type="button">Open Booklet</button
    >
  </div>

  <div
    class="corner-accent corner-accent--top pointer-events-none absolute left-8 top-8 size-12 border-l border-t border-[rgba(140,10,23,0.5)]"
    aria-hidden="true"
  >
  </div>
  <div
    class="corner-accent corner-accent--bottom pointer-events-none absolute bottom-8 right-8 size-12 border-b border-r border-[rgba(140,10,23,0.5)]"
    aria-hidden="true"
  >
  </div>
</section>
